<% if (title) { %>
    <h1><%= title %></h1>
    <% } else { %>
      <script>res.redirect('/')</script>
    <% } %>
    
    <%- messages() %>
    
    <% if (grid) { %>
      <%- grid %>
    <% } else { %>
       <p class="notice">Sorry, no matching vehicles were found.</p>
    <% } %>